#include <windows.h>
#include <iostream>
#include <string.h>

//std::string GetPackageUserModelId(std::wstring *packageName)
//{
//    return std::string(wstring(packageName).begin(), std::wstring(userModelId).end());
//}